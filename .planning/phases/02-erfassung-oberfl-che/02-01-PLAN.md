---
phase: 02-erfassung-oberfl-che
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/main.ts
  - src/ui/state.ts
  - src/ui/format.ts
  - src/ui/render.ts
  - src/styles.css
autonomous: true
requirements:
  - PERS-01
  - PERS-02
  - EXP-01
  - EXP-03
  - UI-01
  - UI-02
  - UI-03
must_haves:
  truths:
    - "Nutzer kann auf einer einzigen durchscrollbaren Seite (kein Routing) eine Person mit Namen hinzufügen und einzeln per ×-Button entfernen (PERS-01, PERS-02, UI-01)."
    - "Nutzer kann eine Ausgabe mit zahlender Person (Select aus erfassten Personen), Beschreibung und Euro-Betrag hinzufügen und einzeln per ×-Button entfernen (EXP-01, EXP-03)."
    - "Nutzer sieht nach jeder Mutation live Gesamtkosten, fairen Anteil, Saldo pro Person und die vorgeschlagenen Ausgleichszahlungen verstaendlich angezeigt (UI-02)."
    - "Ungueltige Eingaben (negativer Betrag, fehlende zahlende Person, weniger als 2 Personen) werden mit dem Wortlaut aus ValidationError.message abgewiesen; der Ergebnisbereich zeigt einen Platzhalter statt Zahlen (UI-03)."
  artifacts:
    - index.html
    - src/main.ts
    - src/ui/state.ts
    - src/ui/format.ts
    - src/ui/render.ts
    - src/styles.css
  key_links:
    - "Die UI importiert ausschliesslich aus src/core/index.ts (Barrel) — keine Direkt-Imports einzelner Kernmodule."
    - "validateSettlementInput steuert die Anzeige; calculateSettlement wird nur bei ok:true aufgerufen und gerendert."
    - "PersonBalance.personId / Transfer.fromId / Transfer.toId werden fuer die Anzeige auf Person.name gemappt."
    - "src/main.ts importiert @fontsource/inter und src/styles.css ins Bundle (self-hosted, keine externen CDNs)."
---

<objective>
Baut die einzige UI-Schicht von PartyPayback: eine Vanilla-DOM Single-Page-Oberflaeche (kein Framework, kein Routing, kein State-Lib, In-Memory), die den fertigen Berechnungskern aus Phase 1 anbindet und das Abrechnungsergebnis live mit Validierungsfeedback anzeigt.

Purpose: Macht den getesteten Berechnungskern bedienbar — Nutzer erfassen Personen und Ausgaben, entfernen sie einzeln und sehen Gesamtkosten, fairen Anteil, Salden und Ausgleichszahlungen nachvollziehbar.

Output: index.html (Vite-Entry), src/main.ts (Entry + Bundle-Imports), src/ui/state.ts (In-Memory-State + Mutationen), src/ui/format.ts (Euro-/Saldo-Formatierung + Name-Mapping), src/ui/render.ts (DOM-Rendering), src/styles.css (handgeschriebenes Card-Layout). KEINE neue Rechen-/Validierungslogik.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-erfassung-oberfl-che/02-CONTEXT.md

# Kern-API (einzige erlaubte Importquelle der UI)
@src/core/index.ts
@src/core/types.ts
@src/core/validation.ts
@src/core/settlement.ts

# Toolchain
@package.json
@vite.config.ts
</context>

<artifacts_this_phase_produces>
Neue Dateien und ihre exportierten/zentralen Symbole:

- `index.html` — Vite-Entry. Statisches Geruest mit drei Sektionen (Personen, Ausgaben, Ergebnis), je ein Formular und ein Listen-/Anzeige-Container mit stabilen `id`-Attributen. `<script type="module" src="/src/main.ts">`.
- `src/main.ts` — App-Entry. Importiert `@fontsource/inter` und `./styles.css` (Bundle), verdrahtet Formulare/Buttons an die State-Mutationen, ruft initiales Render auf.
- `src/ui/state.ts` — In-Memory-State + Mutationen. Exporte: `getPersons()`, `getExpenses()`, `addPerson(name)`, `removePerson(id)`, `addExpense(payerId, description, amount)`, `removeExpense(id)`, `subscribe(listener)`. IDs via `crypto.randomUUID()`. Importiert Typen `Person`, `Expense` NUR aus `../core/index`.
- `src/ui/format.ts` — Anzeige-Helfer. Exporte: `formatEuro(amount)` (2 Nachkommastellen + Euro-Zeichen), `formatBalance(balance)` ("bekommt X € zurueck" / "muss X € zahlen" / "ist ausgeglichen"), `nameOf(personId, persons)` (ID→Name-Mapping).
- `src/ui/render.ts` — DOM-Rendering. Exporte: `renderPersons(...)`, `renderExpenses(...)`, `renderResult(...)`. Ruft `validateSettlementInput` und (nur bei ok:true) `calculateSettlement` aus `../core/index`.
- `src/styles.css` — Handgeschriebenes CSS, helles Card-Layout, Inter, Salden-Farbunterscheidung.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: HTML-Geruest und handgeschriebenes CSS-Card-Layout</name>
  <files>index.html, src/styles.css</files>
  <read_first>
    - src/core/index.ts (Kontext: welche Daten spaeter gerendert werden)
    - .planning/phases/02-erfassung-oberfl-che/02-CONTEXT.md (D-01, D-05, D-06, D-07, D-08, D-09, D-10)
    - package.json (bestaetigt @fontsource/inter als Dependency; type: module)
  </read_first>
  <action>
    Erstelle `index.html` als Vite-Entry mit lang="de", UTF-8, viewport-meta, Titel "PartyPayback" und einem `<script type="module" src="/src/main.ts">` am Body-Ende (D-01). Struktur: einspaltig, vertikal gestapelt, max-width-zentrierter Container in fester Reihenfolge drei `<section>`: Personen → Ausgaben → Ergebnis.

    Personen-Sektion (D-05): `<form id="person-form">` mit `<input id="person-name">` (Text, required, Platzhalter "Name") und `<button type="submit">Hinzufuegen</button>`; darunter ein leerer Listen-Container `<ul id="person-list">` und ein Inline-Fehlercontainer `<div id="person-error">`.

    Ausgaben-Sektion (D-06, D-07): `<form id="expense-form">` mit `<select id="expense-payer">` (zunaechst leer, wird aus Personen befuellt), `<input id="expense-description">` (Text, Platzhalter "Beschreibung"), `<input id="expense-amount" type="number" min="0" step="0.01">` und Submit-Button; darunter `<ul id="expense-list">` und `<div id="expense-error">`.

    Ergebnis-Sektion (D-08): Container `<div id="result">`, der initial leer bleibt (Inhalt rendert src/ui/render.ts) — KEINE Beispieldaten, KEINE leere Flaeche ohne Hinweis (der Platzhaltertext kommt zur Laufzeit aus dem Renderer).

    Erstelle `src/styles.css` als handgeschriebenes, helles, minimal-cleanes Card-Layout (D-09): CSS-Variablen fuer Farbpalette/Spacing/Radien; `font-family` auf Inter mit System-Fallback (die Inter-Schriftdateien werden in Task 3 via @fontsource/inter-Import ins Bundle gezogen — hier nur die font-family-Deklaration, KEIN @import/`<link>` auf externe URLs, BRANDAD/CSP-Constraint); jede Sektion als Card mit dezentem Schatten/Radius; klare Typo-Hierarchie; Formular- und Listen-Styles inkl. Inline-×-Entfernen-Button je Zeile. Salden-Farbunterscheidung (D-10): zwei Utility-Klassen, eine fuer positiv (bekommt zurueck) und eine fuer negativ (muss zahlen), visuell klar abgesetzt. Eine separate Klasse fuer den Ergebnis-Platzhalter (gedaempft).

    Alle Texte mit korrekten deutschen Umlauten (ae/oe/ue/ss sind verboten).
  </action>
  <verify>
    <automated>for id in person-form person-name person-list person-error expense-form expense-payer expense-description expense-amount expense-list expense-error result; do grep -q "id=\"$id\"" index.html || { echo "MISSING id: $id"; exit 1; }; done; grep -q '/src/main.ts' index.html || { echo "MISSING main.ts script"; exit 1; }; echo "index.html ids OK"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `index.html` enthaelt alle 11 stabilen ids (person-form, person-name, person-list, person-error, expense-form, expense-payer, expense-description, expense-amount, expense-list, expense-error, result) und das Modul-Script auf /src/main.ts.
    - SOURCE: `index.html` `<input id="expense-amount">` traegt `type="number"`, `min="0"` und `step="0.01"` (D-07).
    - SOURCE: `index.html` und `src/styles.css` enthalten KEINE externen URLs (kein `fonts.googleapis.com`, `fonts.gstatic.com`, kein `<link rel="stylesheet" href="http">`, kein `@import url("http`).
    - COMMAND: Die Verify-node-Pruefung exit 0.
    - BEHAVIOR: Drei Sektionen in der Reihenfolge Personen → Ausgaben → Ergebnis, einspaltig, max-width-zentriert.
  </acceptance_criteria>
  <done>index.html und src/styles.css existieren mit allen stabilen ids, korrektem number-Input, Card-Layout, Salden-Farbklassen und ohne externe Asset-URLs.</done>
</task>

<task type="auto">
  <name>Task 2: In-Memory-State, Anzeige-Formatierung und DOM-Rendering gegen den Kern</name>
  <files>src/ui/state.ts, src/ui/format.ts, src/ui/render.ts</files>
  <read_first>
    - src/core/index.ts (einzige erlaubte Importquelle: Typen + calculateSettlement + validateSettlementInput)
    - src/core/types.ts (Person, Expense, PersonBalance, Transfer, SettlementResult, ValidationError, ValidationResult)
    - src/core/validation.ts (Wortlaut/Codes der ValidationError.message — werden 1:1 angezeigt)
    - src/core/settlement.ts (calculateSettlement-Signatur, SettlementValidationError)
    - .planning/phases/02-erfassung-oberfl-che/02-CONTEXT.md (D-02, D-03, D-04, D-10, Specifics)
  </read_first>
  <action>
    Erstelle `src/ui/state.ts`: zwei Modul-private Arrays `Person[]` und `Expense[]` (Typen NUR aus `../core/index` importieren, D-Discretion Barrel-Disziplin). Exportiere reine Getter `getPersons()`/`getExpenses()` (Kopien zurueckgeben) und Mutationen `addPerson(name)`, `removePerson(id)`, `addExpense(payerId, description, amount)`, `removeExpense(id)`. IDs via `crypto.randomUUID()`. `addExpense` speichert `amount` als Number unveraendert (keine eigene Validierung — Fachregel bleibt im Kern, D-07/D-04). Implementiere ein einfaches Listener-Muster `subscribe(listener)`, das jede Mutation auf alle Listener feuert, damit main.ts nach jeder Mutation live neu rendert (D-02). Beim Entfernen einer Person die zugehoerigen Ausgaben NICHT automatisch loeschen — der Kern meldet `PAYER_NOT_REGISTERED` und die UI zeigt das an (haelt Single-Source-of-Truth beim Kern; D-03/D-04).

    Erstelle `src/ui/format.ts`: `formatEuro(amount)` formatiert eine Zahl mit genau 2 Nachkommastellen und Euro-Zeichen (deutsche Schreibweise, z. B. via `Intl.NumberFormat('de-DE', { style:'currency', currency:'EUR' })`). `formatBalance(balance)` liefert lesbaren Text gemaess Specifics/D-10: positiv → "bekommt {formatEuro} zurueck", negativ → "muss {formatEuro(Betrag)} zahlen", exakt 0 → "ist ausgeglichen". `nameOf(personId, persons)` mappt eine personId auf `Person.name` (Fallback auf die ID, falls nicht gefunden) — fuer Salden- und Transfer-Anzeige.

    Erstelle `src/ui/render.ts` mit drei exportierten Funktionen, die jeweils einen Ziel-Container und den aktuellen State entgegennehmen:
      - `renderPersons(container, persons, onRemove)`: rendert je Person eine Listenzeile mit Name und ×-Entfernen-Button, der `onRemove(person.id)` aufruft (PERS-02).
      - `renderExpenses(listContainer, payerSelect, persons, expenses, onRemove)`: befuellt das `<select>` der zahlenden Person aus `persons` (Option value=id, Text=name) und rendert je Ausgabe eine Zeile "{nameOf(payerId)} — {description}: {formatEuro(amount)}" mit ×-Button, der `onRemove(expense.id)` aufruft (EXP-01/EXP-03).
      - `renderResult(resultContainer, errorContainers, persons, expenses)`: ruft `validateSettlementInput(persons, expenses)` aus `../core/index` (D-04). Bei `ok:false`: zeige die Fehlertexte aus `error.message` 1:1 kontextnah an (TOO_FEW_PERSONS und allgemeine Hinweise im Ergebnisbereich-Platzhalter, NEGATIVE_AMOUNT/PAYER_NOT_REGISTERED im Ausgaben-Fehlercontainer) UND setze den Ergebnisbereich auf einen freundlichen Platzhalter statt Zahlen (D-03, D-08) — bei zu wenigen Personen z. B. "Mindestens 2 Personen erfassen, um die Abrechnung zu sehen". Bei `ok:true`: rufe `calculateSettlement(persons, expenses)` und rendere Gesamtkosten, fairen Anteil, je Person den Saldo via `formatBalance` (mit Farbklasse aus styles.css, D-10) und die Ausgleichszahlungen als Saetze "{nameOf(fromId)} zahlt {formatEuro(amount)} an {nameOf(toId)}" (UI-02). Leere Fehlercontainer bei ok:true.

    Wichtig: NUR aus `../core/index` importieren (kein Direkt-Import aus types/validation/settlement/money). Alle UI-Texte mit korrekten deutschen Umlauten.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: state.ts, format.ts und render.ts importieren ausschliesslich aus `../core/index` (keine `from '../core/types'` / `'../core/validation'` / `'../core/settlement'` / `'../core/money'`).
    - SOURCE: state.ts verwendet `crypto.randomUUID()` zur ID-Erzeugung.
    - SOURCE: render.ts ruft `validateSettlementInput` und ruft `calculateSettlement` nur im ok:true-Zweig auf.
    - SOURCE: render.ts zeigt Fehlertexte aus `ValidationError.message` an, ohne sie neu zu formulieren (kein hartkodierter Ersatztext fuer NEGATIVE_AMOUNT/PAYER_NOT_REGISTERED).
    - COMMAND: `npm run typecheck` exit 0.
    - BEHAVIOR: Salden werden als "bekommt X € zurueck" / "muss X € zahlen" / "ist ausgeglichen", Transfers als "A zahlt X € an B" gerendert.
  </acceptance_criteria>
  <done>state.ts, format.ts und render.ts existieren, importieren nur via Barrel, binden den Kern an und kompilieren typsauber.</done>
</task>

<task type="auto">
  <name>Task 3: App-Entry verdrahten, Bundle-Imports und Live-Re-Render</name>
  <files>src/main.ts</files>
  <read_first>
    - index.html (stabile ids der Formulare/Buttons/Container aus Task 1)
    - src/ui/state.ts (Mutationen + subscribe aus Task 2)
    - src/ui/render.ts (renderPersons/renderExpenses/renderResult aus Task 2)
    - src/ui/format.ts (Formatierungshelfer aus Task 2)
    - .planning/phases/02-erfassung-oberfl-che/02-CONTEXT.md (D-02, D-09, Specifics)
  </read_first>
  <action>
    Erstelle `src/main.ts` als App-Entry. Importiere als erstes `@fontsource/inter` und `./styles.css`, damit Schrift und Stylesheet ins Bundle gezogen werden (self-hosted, CSP-konform — keine externen CDNs). Greife die DOM-Elemente per ihren stabilen ids aus index.html ab (typsicher, mit Null-Guard bzw. non-null-Assertion und sprechender Fehlermeldung falls fehlend).

    Verdrahte das Personen-Formular: bei submit `preventDefault`, trimme den Namen, bei leerem Namen nicht hinzufuegen, sonst `addPerson(name)`; danach Feld leeren und Fokus zurueck ins Namensfeld (Specifics). Verdrahte das Ausgaben-Formular: bei submit `preventDefault`, lies payerId aus dem Select, Beschreibung und Betrag (Betrag via `parseFloat`/`Number`); rufe `addExpense(payerId, description, amount)`; danach Felder leeren und Fokus zurueck ins erste Ausgabenfeld (Specifics). Die fachliche Pruefung negativer Betraege bleibt im Kern und wird ueber den Renderer angezeigt (D-07).

    Registriere via `subscribe(...)` einen Re-Render-Callback, der nach JEDER Mutation `renderPersons`, `renderExpenses` und `renderResult` mit dem aktuellen State (`getPersons()`/`getExpenses()`) aufruft (D-02, kein Berechnen-Button). Uebergib die Remove-Callbacks (`removePerson`, `removeExpense`) an die Render-Funktionen. Fuehre einmal initial ein Render aus, damit der Ergebnis-Platzhalter (D-08) und das leere Payer-Select direkt sichtbar sind.

    Alle UI-Texte/Kommentare mit korrekten deutschen Umlauten.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: src/main.ts importiert `@fontsource/inter` und `./styles.css` (Bundle-Imports), und importiert State-/Render-Funktionen aus `./ui/state` und `./ui/render`.
    - SOURCE: main.ts ruft `preventDefault` auf beiden Formularen, leert die Felder nach dem Hinzufuegen und setzt den Fokus zurueck (Specifics).
    - SOURCE: main.ts registriert `subscribe` und rendert alle drei Bereiche im Callback (Live-Re-Render, D-02); kein "Berechnen"-Button im Code referenziert.
    - SOURCE: main.ts enthaelt keine externen URL-Imports (kein http/https in Import-Statements).
    - COMMAND: `npm run typecheck` exit 0.
    - COMMAND: `npm run build` exit 0 (Vite-Bundle erfolgreich, Inter-Schrift gebundelt).
  </acceptance_criteria>
  <done>src/main.ts verdrahtet Formulare, Mutationen und Live-Re-Render; npm run typecheck und npm run build laufen sauber durch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nutzer → DOM-Formulare | Frei eingegebene Namen, Beschreibungen, Betraege; rein clientseitig, In-Memory, keine Persistenz, kein Backend. |
| UI → Berechnungskern (src/core) | UI uebergibt Person[]/Expense[] an den getesteten Kern; Fachregeln werden ausschliesslich im Kern durchgesetzt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Frei eingegebene Namen/Beschreibungen → DOM | low | mitigate | Werte ausschliesslich via `textContent`/DOM-API setzen, nie via `innerHTML` mit User-Daten — kein XSS-Vektor (rein clientseitige, In-Memory-App ohne Persistenz/Sharing). |
| T-02-02 | Tampering | Betrags-Eingabe (number-Input) | low | accept | HTML `min="0"`/`step="0.01"` ist nur UX; die fachliche Ablehnung negativer Betraege erfolgt im Kern (EXP-04) und wird via ValidationError.message angezeigt. Akzeptiert, weil der Kern die einzige Autoritaet ist. |
| T-02-03 | Information Disclosure | Externe Asset-Egress (Fonts/CDN) | medium | mitigate | Inter ausschliesslich via `@fontsource/inter` self-hosted ins Bundle; keine externen `<link>`/`@import`/CDN-URLs (BRANDAD-CSP/DSGVO-Constraint). Acceptance-Criteria pruefen das Fehlen externer URLs. |
</threat_model>

<verification>
Gesamt-Phasen-Checks (alle automatisiert + manuelle Sichtpruefung der 3 Success Criteria):

- `npm run typecheck` exit 0.
- `npm run build` exit 0 (Inter gebundelt, keine externen Asset-Fehler).
- Barrel-Disziplin: kein UI-Modul importiert direkt aus core/types|validation|settlement|money — nur aus core/index.
- Keine externen Asset-URLs in index.html, styles.css, main.ts.
- Manuelle Sichtpruefung (`npm run dev`) der 3 ROADMAP-Success-Criteria:
  1. Person mit Name hinzufuegen + via × entfernen; Ausgabe mit Payer-Select/Beschreibung/Betrag hinzufuegen + via × entfernen — alles auf einer Seite ohne Routing.
  2. Bei >= 2 Personen + Ausgaben: Gesamtkosten, fairer Anteil, Saldo pro Person (mit Farbunterscheidung), Ausgleichszahlungen als lesbare Saetze sichtbar.
  3. Negativer Betrag / fehlende zahlende Person / < 2 Personen → nachvollziehbare Fehlertexte (Wortlaut aus dem Kern) + Ergebnis-Platzhalter statt Zahlen.
</verification>

<success_criteria>
- Nutzer kann auf einer einzigen Seite Personen und Ausgaben hinzufuegen und einzeln entfernen (PERS-01, PERS-02, EXP-01, EXP-03, UI-01).
- Nutzer sieht live Gesamtkosten, fairen Anteil, Saldo pro Person und Ausgleichszahlungen verstaendlich (UI-02).
- Ungueltige Eingaben werden mit dem Wortlaut des Kerns abgewiesen, Ergebnisbereich zeigt Platzhalter (UI-03).
- `npm run typecheck` und `npm run build` exit 0; keine externen Asset-URLs; UI importiert nur via core/index.
</success_criteria>

<output>
Create `.planning/phases/02-erfassung-oberfl-che/02-01-SUMMARY.md` when done
</output>
